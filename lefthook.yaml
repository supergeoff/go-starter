# pre-commit:
#   parallel: true
#   commands:
#     format:
#       run: golangci-lint fmt ./...
#     lint:
#       run: mage Lint
commit-msg:
  commands:
    conventional:
       run: ./tools/commitlint.sh {1}